# 链动货源查询手机安装包路线图

## 目标与边界

- 第一阶段交付 Android 10+ 的 APK 测试包和可上架使用的 AAB，iOS 在 Android 稳定后评估。
- 复用现有 React 查询、筛选、排序、收藏、查询方案、三天价格历史、商品关联和蓝色主题。
- 商品详情通过 Android 默认浏览器打开；移动端不实现快捷购买、自动填表、创建订单或支付跳转。

## 技术路线

- 使用 Capacitor 包装共享 React 界面，桌面 Electron 主进程能力抽象成统一的 `PlatformBridge`。
- Android 端通过原生 HTTPS 桥访问货源接口，避免 WebView CORS 问题；登录使用隔离的官方 WebView，并只提取平台签发的登录令牌。
- 登录令牌使用 Android Keystore 加密；渲染页面只获得登录状态和脱敏摘要。
- 商品关联与取消关联复用桌面端的运行时校验、明确确认和状态回写，只允许用户单次操作。
- 商品详情只允许链动小铺官方 HTTPS 域名，并交给系统默认浏览器打开。
- 收藏、方案和历史使用 Android 本地文件或 Room 数据库保存；监控同样只比较完整成功的全页结果。

## 实施阶段

1. 抽离共享 UI、筛选器、商品类型和运行时校验，保持 Windows 功能不变。
2. 建立 Capacitor Android 工程，实现安全存储、官方登录、货源请求、关联操作和外部浏览器桥。
3. 完成手机列表卡片、筛选抽屉、进度与取消、详情跳转和断网会话恢复。
4. 在真实 Android 设备验证登录、100 页快速拉取、429 冷却、系统浏览器跳转和后台恢复。
5. 生成签名 APK/AAB，补充隐私说明、更新机制和安装升级测试。

## 验收标准

- Android 与 Windows 对同一关键词、商品类型和页数返回一致的去重结果。
- 应用重启或短时断网不丢登录；401/403 才要求重新登录。
- 关联和取消关联必须由用户逐项确认，不能批量或后台静默执行。
- 登录令牌不暴露给 React 页面；应用不收集或保存购买联系方式和安全密码。
- APK 目标体积控制在 25 MB 左右；实际大小以原生依赖和签名后的产物为准。
